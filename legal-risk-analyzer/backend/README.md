# Backend

Install backend dependencies with:

```
pip install -r requirements.txt
```

The `requirements.txt` already includes `python-multipart`, which is required for the `/upload` endpoint (form/file uploads).
